# Sales Analytics System

## Overview
This project processes messy sales data, integrates external API product data,
performs analytics, and generates a professional sales report.

## Setup
```bash
pip install -r requirements.txt
